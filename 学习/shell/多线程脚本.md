
```
mkfifo {tmp_fifo_file}
exec 6 <>${tmp_fifo_file}
thread_number=1
for ((i=0;i<${thread_number};i++));
do
    echo
done >&6
while read line 
do
    read -u6
   {echo ${line}}&
done < ${tablelist}
```
